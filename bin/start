#!/usr/bin/env sh

execPath=$(pwd)

scriptPath=$(cd $(dirname $0); pwd)

echo "$scriptPath/../server.js>server.out"

nohup node "$scriptPath/../server.js">"$scriptPath/server.out" 2>&1 &
nohup node "$scriptPath/../client.js">"$scriptPath/client.out" 2>&1 &

exit
